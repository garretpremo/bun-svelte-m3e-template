<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/badge");
  import type { M3eBadgeElement, BadgeSize } from "@m3e/badge";

  interface Props {
    /** The size of the badge. */
    size?: BadgeSize;
    /** The position of the badge, when attached to another element. */
    position?: string;
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    /** Renders the content of the badge. */
    children?: Snippet;
    element?: M3eBadgeElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { size, position, for_, children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-badge
  bind:this={element}
  {...rest}
  {size}
  {position}
  for={for_}
>
  {@render children?.()}
</m3e-badge>
