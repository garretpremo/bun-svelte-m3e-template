<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/core");
  import type { M3eElevationElement, ElevationLevel } from "@m3e/core";

  interface Props {
    /** Whether hover and press events will not trigger changes in elevation, when attached to an interactive element. */
    disabled?: boolean;
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    /** The level at which to visually depict elevation. */
    level?: ElevationLevel | null;
    element?: M3eElevationElement;
  }

  let { disabled, for_, level, element = $bindable() }: Props = $props();
</script>

<m3e-elevation
  bind:this={element}
  disabled={disabled || undefined}
  for={for_}
  {level}
>

</m3e-elevation>
