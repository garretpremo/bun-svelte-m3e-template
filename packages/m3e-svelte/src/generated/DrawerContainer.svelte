<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/drawer-container");
  import type { M3eDrawerContainerElement, DrawerMode } from "@m3e/drawer-container";

  interface Props {
    /** Whether the end drawer is open. */
    end?: boolean;
    /** The behavior mode of the end drawer. */
    endMode?: DrawerMode;
    /** Whether to show a divider between the end drawer and content for `side` mode. */
    endDivider?: boolean;
    /** Whether the start drawer is open. */
    start?: boolean;
    /** The behavior mode of the start drawer. */
    startMode?: DrawerMode;
    /** Whether to show a divider between the start drawer and content for `side` mode. */
    startDivider?: boolean;
    /** Renders the main content. */
    children?: Snippet;
    /** Renders the start drawer. */
    startSnippet?: Snippet;
    /** Renders the end drawer. */
    endSnippet?: Snippet;
    /** Emitted when the state of the start or end drawers change. */
    onchange?: (e: Event) => void;
    element?: M3eDrawerContainerElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { end, endMode, endDivider, start, startMode, startDivider, children, startSnippet, endSnippet, onchange, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-drawer-container
  bind:this={element}
  {...rest}
  end={end || undefined}
  end-mode={endMode}
  end-divider={endDivider || undefined}
  start={start || undefined}
  start-mode={startMode}
  start-divider={startDivider || undefined}
  onchange={onchange}
>
  {#if startSnippet}<div slot="start" style="display:contents">{@render startSnippet()}</div>{/if}
  {#if endSnippet}<div slot="end" style="display:contents">{@render endSnippet()}</div>{/if}
  {@render children?.()}
</m3e-drawer-container>
