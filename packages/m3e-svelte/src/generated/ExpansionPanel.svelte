<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  import { syncProperty } from "../runtime/upgrade";
  if (browser) void import("@m3e/expansion-panel");
  import type { M3eExpansionPanelElement, ExpansionToggleDirection, ExpansionTogglePosition } from "@m3e/expansion-panel";

  interface Props {
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** Whether to hide the expansion toggle. */
    hideToggle?: boolean;
    /** The direction of the expansion toggle. */
    toggleDirection?: ExpansionToggleDirection;
    /** The position of the expansion toggle. */
    togglePosition?: ExpansionTogglePosition;
    /** Whether the panel is expanded. */
    open?: boolean;
    /** Renders the detail of the panel. */
    children?: Snippet;
    /** Renders the actions bar of the panel. */
    actions?: Snippet;
    /** Renders the header content. */
    header?: Snippet;
    /** Renders the expansion toggle icon. */
    toggleIcon?: Snippet;
    /** Emitted when the expansion panel begins to open. */
    onopening?: (e: Event) => void;
    /** Emitted when the expansion panel has opened. */
    onopened?: (e: Event) => void;
    /** Emitted when the expansion panel begins to close. */
    onclosing?: (e: Event) => void;
    /** Emitted when the expansion panel has closed. */
    onclosed?: (e: Event) => void;
    element?: M3eExpansionPanelElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disabled, hideToggle, toggleDirection, togglePosition, open = $bindable(false), children, actions, header, toggleIcon, onopening, onopened, onclosing, onclosed, element = $bindable(), ...rest }: Props = $props();

  $effect(() => {
    if (open !== undefined) syncProperty(element, "open", open);
  });
</script>

<m3e-expansion-panel
  bind:this={element}
  {...rest}
  disabled={disabled || undefined}
  hide-toggle={hideToggle || undefined}
  toggle-direction={toggleDirection}
  toggle-position={togglePosition}
  onopening={onopening}
  onopened={onopened}
  onclosing={onclosing}
  onclosed={onclosed}
>
  {#if actions}<div slot="actions" style="display:contents">{@render actions()}</div>{/if}
  {#if header}<div slot="header" style="display:contents">{@render header()}</div>{/if}
  {#if toggleIcon}<div slot="toggle-icon" style="display:contents">{@render toggleIcon()}</div>{/if}
  {@render children?.()}
</m3e-expansion-panel>
