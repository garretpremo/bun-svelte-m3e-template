<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  import { syncManagedProperty } from "../runtime/upgrade";
  if (browser) void import("@m3e/radio-group");
  import type { M3eRadioGroupElement } from "@m3e/radio-group";

  interface Props {
    ariaInvalid?: string;
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** The name that identifies the element when submitting the associated form. */
    name?: string;
    /** Whether the element is required. */
    required?: boolean;
    /** Renders the radio buttons of the group. */
    children?: Snippet;
    /** Emitted when the checked state of a radio button changes. */
    onchange?: (e: Event) => void;
    element?: M3eRadioGroupElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { ariaInvalid, disabled, name, required, children, onchange, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-radio-group
  bind:this={element}
  {...rest}
  aria-invalid={ariaInvalid}
  disabled={disabled || undefined}
  {name}
  required={required || undefined}
  onchange={onchange}
>
  {@render children?.()}
</m3e-radio-group>
