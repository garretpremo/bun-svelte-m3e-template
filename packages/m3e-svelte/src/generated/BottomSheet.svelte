<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  import { syncProperty } from "../runtime/upgrade";
  if (browser) void import("@m3e/bottom-sheet");
  import type { M3eBottomSheetElement } from "@m3e/bottom-sheet";

  interface Props {
    /** The zero‑based index of the detent the sheet should open to. */
    detent?: number;
    /** Detents (discrete height states) the sheet can snap to. */
    detents?: string[];
    /** Whether to display a drag handle and enable the top region of the sheet as a gesture surface for dragging between detents. */
    handle?: boolean;
    /** The accessible label given to the drag handle. */
    handleLabel?: string;
    /** Whether the bottom sheet can hide when its swiped down. */
    hideable?: boolean;
    /** The friction coefficient to hide the sheet, or set it to the next closest expanded detent. */
    hideFriction?: number;
    /** Whether the bottom sheet behaves as modal. */
    modal?: boolean;
    /** Whether the bottom sheet is open. */
    open?: boolean;
    /** Renders the content of the sheet. */
    children?: Snippet;
    /** Renders the header of the sheet. */
    header?: Snippet;
    /** Emitted when the sheet begins to open. */
    onopening?: (e: Event) => void;
    /** Emitted when the sheet begins to close. */
    onclosing?: (e: Event) => void;
    /** Emitted when the sheet is cancelled. */
    oncancel?: (e: Event) => void;
    /** Emitted when the sheet has opened. */
    onopened?: (e: Event) => void;
    /** Emitted when the sheet has closed. */
    onclosed?: (e: Event) => void;
    element?: M3eBottomSheetElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { detent, detents, handle, handleLabel, hideable, hideFriction, modal, open = $bindable(false), children, header, onopening, onclosing, oncancel, onopened, onclosed, element = $bindable(), ...rest }: Props = $props();

  $effect(() => {
    if (open !== undefined) syncProperty(element, "open", open);
  });
</script>

<m3e-bottom-sheet
  bind:this={element}
  {...rest}
  {detent}
  {detents}
  handle={handle || undefined}
  handle-label={handleLabel}
  hideable={hideable || undefined}
  hide-friction={hideFriction}
  modal={modal || undefined}
  onopening={onopening}
  onclosing={onclosing}
  oncancel={oncancel}
  onopened={onopened}
  onclosed={onclosed}
>
  {#if header}<div slot="header" style="display:contents">{@render header()}</div>{/if}
  {@render children?.()}
</m3e-bottom-sheet>
