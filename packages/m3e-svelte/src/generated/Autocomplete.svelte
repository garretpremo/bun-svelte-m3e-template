<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/autocomplete");
  import type { M3eAutocompleteElement } from "@m3e/autocomplete";

  interface Props {
    /** Whether the first option should be automatically activated. */
    autoActivate?: boolean;
    /** Whether to hide the selection indicator. */
    hideSelectionIndicator?: boolean;
    /** Whether the user is required to make a selection when interacting with the autocomplete. */
    required?: boolean;
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    /** Renders the options of the autocomplete. */
    children?: Snippet;
    ontoggle?: (e: ToggleEvent) => void;
    element?: M3eAutocompleteElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { autoActivate, hideSelectionIndicator, required, for_, children, ontoggle, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-autocomplete
  bind:this={element}
  {...rest}
  auto-activate={autoActivate || undefined}
  hide-selection-indicator={hideSelectionIndicator || undefined}
  required={required || undefined}
  for={for_}
  ontoggle={ontoggle}
>
  {@render children?.()}
</m3e-autocomplete>
