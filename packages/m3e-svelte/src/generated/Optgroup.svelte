<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/option");
  import type { M3eOptGroupElement } from "@m3e/option";

  interface Props {
    /** Renders the options of the group. */
    children?: Snippet;
    /** Renders the label of the group. */
    label?: Snippet;
    element?: M3eOptGroupElement;
  }

  let { children, label, element = $bindable() }: Props = $props();
</script>

<m3e-optgroup
  bind:this={element}
>
  {#if label}<div slot="label" style="display:contents">{@render label()}</div>{/if}
  {@render children?.()}
</m3e-optgroup>
