<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/core");
  import type { M3eStateLayerElement } from "@m3e/core";

  interface Props {
    /** Whether hover and focus events will not trigger the state layer. State layers can still be controlled manually using the `show` and `hide` methods. */
    disabled?: boolean;
    /** Whether hover events will not trigger the state layer. State layers can still be controlled manually using the `show` and `hide` methods. */
    disableHover?: boolean;
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    element?: M3eStateLayerElement;
  }

  let { disabled, disableHover, for_, element = $bindable() }: Props = $props();
</script>

<m3e-state-layer
  bind:this={element}
  disabled={disabled || undefined}
  disable-hover={disableHover || undefined}
  for={for_}
>

</m3e-state-layer>
