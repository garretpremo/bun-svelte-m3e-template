<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  import { syncManagedProperty } from "../runtime/upgrade";
  if (browser) void import("@m3e/list");
  import type { M3eActionListElement, ListVariant } from "@m3e/list";

  interface Props {
    /** The appearance variant of the list. */
    variant?: ListVariant;
    /** Renders the items of the list. */
    children?: Snippet;
    element?: M3eActionListElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { variant, children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-action-list
  bind:this={element}
  {...rest}
  {variant}
>
  {@render children?.()}
</m3e-action-list>
