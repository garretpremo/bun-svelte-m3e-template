<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/paginator");
  import type { M3ePaginatorElement } from "@m3e/paginator";

  interface Props {
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** The accessible label given to the button used to move to the first page. */
    firstPageLabel?: string;
    /** Whether to hide page size selection. */
    hidePageSize?: boolean;
    /** The label for the page size selector. */
    itemsPerPageLabel?: string;
    /** The accessible label given to the button used to move to the last page. */
    lastPageLabel?: string;
    /** The length of the total number of items which are being paginated. */
    length?: number;
    /** The accessible label given to the button used to move to the next page. */
    nextPageLabel?: string;
    /** The zero-based page index of the displayed list of items. */
    pageIndex?: number;
    /** The number of items to display in a page. */
    pageSize?: number | "all";
    /** A comma separated list of available page sizes. */
    pageSizes?: string;
    /** The appearance variant of the page size field. */
    pageSizeVariant?: string;
    /** The accessible label given to the button used to move to the previous page. */
    previousPageLabel?: string;
    /** Whether to show first/last buttons. */
    showFirstLastButtons?: boolean;
    /** Slot for a custom first-page icon. */
    firstPageIcon?: Snippet;
    /** Slot for a custom previous-page icon. */
    previousPageIcon?: Snippet;
    /** Slot for a custom next-page icon. */
    nextPageIcon?: Snippet;
    /** Slot for a custom last-page icon. */
    lastPageIcon?: Snippet;
    /** Emitted when a user selects a different page size or navigates to another page. */
    onpage?: (e: CustomEvent) => void;
    element?: M3ePaginatorElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disabled, firstPageLabel, hidePageSize, itemsPerPageLabel, lastPageLabel, length, nextPageLabel, pageIndex, pageSize, pageSizes, pageSizeVariant, previousPageLabel, showFirstLastButtons, firstPageIcon, previousPageIcon, nextPageIcon, lastPageIcon, onpage, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-paginator
  bind:this={element}
  {...rest}
  disabled={disabled || undefined}
  first-page-label={firstPageLabel}
  hide-page-size={hidePageSize || undefined}
  items-per-page-label={itemsPerPageLabel}
  last-page-label={lastPageLabel}
  {length}
  next-page-label={nextPageLabel}
  page-index={pageIndex}
  page-size={pageSize}
  page-sizes={pageSizes}
  page-size-variant={pageSizeVariant}
  previous-page-label={previousPageLabel}
  show-first-last-buttons={showFirstLastButtons || undefined}
  onpage={onpage}
>
  {#if firstPageIcon}<div slot="first-page-icon" style="display:contents">{@render firstPageIcon()}</div>{/if}
  {#if previousPageIcon}<div slot="previous-page-icon" style="display:contents">{@render previousPageIcon()}</div>{/if}
  {#if nextPageIcon}<div slot="next-page-icon" style="display:contents">{@render nextPageIcon()}</div>{/if}
  {#if lastPageIcon}<div slot="last-page-icon" style="display:contents">{@render lastPageIcon()}</div>{/if}
</m3e-paginator>
