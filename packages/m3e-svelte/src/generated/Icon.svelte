<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/icon");
  import type { M3eIconElement, IconGrade, IconVariant } from "@m3e/icon";

  interface Props {
    /** Whether the icon is filled. */
    filled?: boolean;
    /** The grade of the icon. */
    grade?: IconGrade;
    /** A value from 20 to 48 indicating the optical size of the icon. */
    opticalSize?: number;
    /** The name of the icon. */
    name?: string;
    /** The appearance variant of the icon. */
    variant?: IconVariant;
    /** A value from 100 to 700 indicating the weight of the icon. */
    weight?: number;
    element?: M3eIconElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { filled, grade, opticalSize, name, variant, weight, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-icon
  bind:this={element}
  {...rest}
  filled={filled || undefined}
  {grade}
  optical-size={opticalSize}
  {name}
  {variant}
  {weight}
>

</m3e-icon>
