<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  import { syncProperty } from "../runtime/upgrade";
  if (browser) void import("@m3e/core");
  import type { M3ePseudoCheckboxElement } from "@m3e/core";

  interface Props {
    /** A value indicating whether the element is disabled. */
    disabled?: boolean;
    /** A value indicating whether the element is checked. */
    checked?: boolean;
    /** A value indicating whether the element's checked state is indeterminate. */
    indeterminate?: boolean;
    element?: M3ePseudoCheckboxElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disabled, checked = $bindable(false), indeterminate = $bindable(false), element = $bindable(), ...rest }: Props = $props();

  $effect(() => {
    if (checked !== undefined) syncProperty(element, "checked", checked);
  });
  $effect(() => {
    if (indeterminate !== undefined) syncProperty(element, "indeterminate", indeterminate);
  });
</script>

<m3e-pseudo-checkbox
  bind:this={element}
  {...rest}
  disabled={disabled || undefined}
>

</m3e-pseudo-checkbox>
