<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/theme");
  import type { M3eThemeElement, ContrastLevel, ColorScheme, MotionScheme } from "@m3e/theme";

  interface Props {
    /** The hex color from which to derive dynamic color palettes. */
    color?: string;
    /** The contrast level of the theme. */
    contrast?: ContrastLevel;
    /** The density scale (0, -1, -2). */
    density?: number;
    /** The color scheme of the theme. */
    scheme?: ColorScheme;
    /** Whether to enable strong focus indicators. */
    strongFocus?: boolean;
    /** The motion scheme. */
    motion?: MotionScheme;
    /** Dispatched when the theme changes. */
    onchange?: (e: Event) => void;
    element?: M3eThemeElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { color, contrast, density, scheme, strongFocus, motion, onchange, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-theme
  bind:this={element}
  {...rest}
  {color}
  {contrast}
  {density}
  {scheme}
  strong-focus={strongFocus || undefined}
  {motion}
  onchange={onchange}
>

</m3e-theme>
