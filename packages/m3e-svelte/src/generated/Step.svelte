<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  import { syncManagedProperty } from "../runtime/upgrade";
  if (browser) void import("@m3e/stepper");
  import type { M3eStepElement } from "@m3e/stepper";

  interface Props {
    /** Whether the step has been completed. */
    completed?: boolean;
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** Whether the step is editable and users can return to it after completion. */
    editable?: boolean;
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    /** Whether the step is optional. */
    optional?: boolean;
    /** Whether the step has an error. */
    invalid?: boolean;
    /** Whether the element is selected. */
    selected?: boolean;
    /** Renders the label of the step. */
    children?: Snippet;
    /** Renders the icon of the step. */
    icon?: Snippet;
    /** Renders the icon of a completed step. */
    doneIcon?: Snippet;
    /** Renders the icon of a completed editable step. */
    editIcon?: Snippet;
    /** Renders icon of an invalid step. */
    errorIcon?: Snippet;
    /** Renders the hint text of the step. */
    hint?: Snippet;
    /** Renders the error message for an invalid step. */
    error?: Snippet;
    /** Emitted when the selected state changes. */
    oninput?: (e: Event) => void;
    /** Emitted when the selected state changes. */
    onchange?: (e: Event) => void;
    /** Emitted when the element is clicked. */
    onclick?: (e: MouseEvent) => void;
    element?: M3eStepElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { completed, disabled, editable, for_, optional, invalid, selected = $bindable(false), children, icon, doneIcon, editIcon, errorIcon, hint, error, oninput, onchange, onclick, element = $bindable(), ...rest }: Props = $props();

  function syncFromDom() {
    if (!element) return;
    const node = element as unknown as Record<string, unknown>;
    { const next = node["selected"]; if (next != null) selected = next as boolean; }
  }

  $effect(() => {
    if (selected !== undefined) syncManagedProperty(element, "selected", selected);
  });
</script>

<!-- svelte-ignore a11y_click_events_have_key_events -->
<!-- svelte-ignore a11y_no_static_element_interactions -->
<m3e-step
  bind:this={element}
  {...rest}
  completed={completed || undefined}
  disabled={disabled || undefined}
  editable={editable || undefined}
  for={for_}
  optional={optional || undefined}
  invalid={invalid || undefined}
  oninput={(e: Event) => { syncFromDom(); oninput?.(e); }}
  onchange={(e: Event) => { syncFromDom(); onchange?.(e); }}
  onclick={onclick}
>
  {#if icon}<div slot="icon" style="display:contents">{@render icon()}</div>{/if}
  {#if doneIcon}<div slot="done-icon" style="display:contents">{@render doneIcon()}</div>{/if}
  {#if editIcon}<div slot="edit-icon" style="display:contents">{@render editIcon()}</div>{/if}
  {#if errorIcon}<div slot="error-icon" style="display:contents">{@render errorIcon()}</div>{/if}
  {#if hint}<div slot="hint" style="display:contents">{@render hint()}</div>{/if}
  {#if error}<div slot="error" style="display:contents">{@render error()}</div>{/if}
  {@render children?.()}
</m3e-step>
