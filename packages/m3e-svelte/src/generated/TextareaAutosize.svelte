<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/textarea-autosize");
  import type { M3eTextareaAutosizeElement } from "@m3e/textarea-autosize";

  interface Props {
    /** Whether auto-sizing is disabled. */
    disabled?: boolean;
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    /** The maximum amount of rows in the `textarea`. */
    maxRows?: number;
    /** The minimum amount of rows in the `textarea`. */
    minRows?: number;
    element?: M3eTextareaAutosizeElement;
  }

  let { disabled, for_, maxRows, minRows, element = $bindable() }: Props = $props();
</script>

<m3e-textarea-autosize
  bind:this={element}
  disabled={disabled || undefined}
  for={for_}
  max-rows={maxRows}
  min-rows={minRows}
>

</m3e-textarea-autosize>
