<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/toc");
  import type { M3eTocElement } from "@m3e/toc";

  interface Props {
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    /** The maximum depth of the table of contents. */
    maxDepth?: number;
    /** Renders content between the header and items. */
    children?: Snippet;
    /** Renders the overline of the table of contents. */
    overline?: Snippet;
    /** Renders the title of the table of contents. */
    title?: Snippet;
    element?: M3eTocElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { for_, maxDepth, children, overline, title, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-toc
  bind:this={element}
  {...rest}
  for={for_}
  max-depth={maxDepth}
>
  {#if overline}<div slot="overline" style="display:contents">{@render overline()}</div>{/if}
  {#if title}<div slot="title" style="display:contents">{@render title()}</div>{/if}
  {@render children?.()}
</m3e-toc>
