<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/expansion-panel");
  import type { M3eExpansionHeaderElement, ExpansionToggleDirection, ExpansionTogglePosition } from "@m3e/expansion-panel";

  interface Props {
    /** Whether to hide the expansion toggle. */
    hideToggle?: boolean;
    /** The direction of the expansion toggle. */
    toggleDirection?: ExpansionToggleDirection;
    /** The position of the expansion toggle. */
    togglePosition?: ExpansionTogglePosition;
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** Renders the content of the header. */
    children?: Snippet;
    /** Renders the icon of the expansion toggle. */
    toggleIcon?: Snippet;
    /** Emitted when the element is clicked. */
    onclick?: (e: MouseEvent) => void;
    element?: M3eExpansionHeaderElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { hideToggle, toggleDirection, togglePosition, disabled, children, toggleIcon, onclick, element = $bindable(), ...rest }: Props = $props();
</script>

<!-- svelte-ignore a11y_click_events_have_key_events -->
<!-- svelte-ignore a11y_no_static_element_interactions -->
<m3e-expansion-header
  bind:this={element}
  {...rest}
  hide-toggle={hideToggle || undefined}
  toggle-direction={toggleDirection}
  toggle-position={togglePosition}
  disabled={disabled || undefined}
  onclick={onclick}
>
  {#if toggleIcon}<div slot="toggle-icon" style="display:contents">{@render toggleIcon()}</div>{/if}
  {@render children?.()}
</m3e-expansion-header>
