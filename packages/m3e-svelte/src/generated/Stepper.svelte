<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/stepper");
  import type { M3eStepperElement, StepHeaderPosition, StepLabelPosition } from "@m3e/stepper";

  interface Props {
    /** The position of the step header, when oriented horizontally. */
    headerPosition?: StepHeaderPosition;
    /** The position of the step labels, when oriented horizontally. */
    labelPosition?: StepLabelPosition;
    /** Whether the validity of previous steps should be checked or not. */
    linear?: boolean;
    /** The orientation of the stepper. */
    orientation?: string;
    /** Renders a step. */
    step?: Snippet;
    /** Renders a panel. */
    panel?: Snippet;
    /** Emitted when the selected step changes. */
    onchange?: (e: Event) => void;
    element?: M3eStepperElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { headerPosition, labelPosition, linear, orientation, step, panel, onchange, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-stepper
  bind:this={element}
  {...rest}
  header-position={headerPosition}
  label-position={labelPosition}
  linear={linear || undefined}
  {orientation}
  onchange={onchange}
>
  {#if step}<div slot="step" style="display:contents">{@render step()}</div>{/if}
  {#if panel}<div slot="panel" style="display:contents">{@render panel()}</div>{/if}
</m3e-stepper>
