<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  import { syncManagedProperty } from "../runtime/upgrade";
  if (browser) void import("@m3e/list");
  import type { M3eListElement, ListVariant } from "@m3e/list";

  interface Props {
    /** The appearance variant of the list. */
    variant?: ListVariant;
    /** Renders the items of the list. */
    children?: Snippet;
    element?: M3eListElement;
  }

  let { variant, children, element = $bindable() }: Props = $props();
</script>

<m3e-list
  bind:this={element}
  {variant}
>
  {@render children?.()}
</m3e-list>
