<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/app-bar");
  import type { M3eAppBarElement, AppBarSize } from "@m3e/app-bar";

  interface Props {
    /** Whether the title and subtitle are centered. */
    centered?: boolean;
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    /** The size of the bar. */
    size?: AppBarSize;
    /** Renders a leading icon. */
    leadingIcon?: Snippet;
    /** Renders the subtitle. */
    subtitle?: Snippet;
    /** Renders the title. */
    title?: Snippet;
    /** Renders a trailing icon. */
    trailingIcon?: Snippet;
    element?: M3eAppBarElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { centered, for_, size, leadingIcon, subtitle, title, trailingIcon, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-app-bar
  bind:this={element}
  {...rest}
  centered={centered || undefined}
  for={for_}
  {size}
>
  {#if leadingIcon}<div slot="leading-icon" style="display:contents">{@render leadingIcon()}</div>{/if}
  {#if subtitle}<div slot="subtitle" style="display:contents">{@render subtitle()}</div>{/if}
  {#if title}<div slot="title" style="display:contents">{@render title()}</div>{/if}
  {#if trailingIcon}<div slot="trailing-icon" style="display:contents">{@render trailingIcon()}</div>{/if}
</m3e-app-bar>
