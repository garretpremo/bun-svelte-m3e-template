<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/progress-indicator");
  import type { M3eCircularProgressIndicatorElement } from "@m3e/progress-indicator";

  interface Props {
    /** Whether to show something is happening without conveying progress. */
    indeterminate?: boolean;
    /** The maximum progress value. */
    max?: number;
    /** A fractional value, between 0 and `max`, indicating progress. */
    value?: number;
    /** The appearance of the indicator. */
    variant?: string;
    /** Renders the content inside the progress indicator. */
    children?: Snippet;
    element?: M3eCircularProgressIndicatorElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { indeterminate, max, value, variant, children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-circular-progress-indicator
  bind:this={element}
  {...rest}
  indeterminate={indeterminate || undefined}
  {max}
  {value}
  {variant}
>
  {@render children?.()}
</m3e-circular-progress-indicator>
