<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/icon-button");
  import type { M3eIconButtonElement, IconButtonShape, IconButtonSize, IconButtonVariant, IconButtonWidth } from "@m3e/icon-button";

  interface Props {
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** Whether the element is disabled and interactive. */
    disabledInteractive?: boolean;
    /** A value indicating whether the `target` of the link button will be downloaded, optionally specifying the new name of the file. */
    download?: string | null;
    /** The URL to which the link button points. */
    href?: string;
    /** The name of the element, submitted as a pair with the element's `value` as part of form data, when the element is used to submit a form. */
    name?: string;
    /** The relationship between the `target` of the link button and the document. */
    rel?: string;
    /** Whether the toggle button is selected. */
    selected?: boolean;
    /** The shape of the button. */
    shape?: IconButtonShape;
    /** The size of the button. */
    size?: IconButtonSize;
    /** The target of the link button. */
    target?: string;
    /** Whether the button will toggle between selected and unselected states. */
    toggle?: boolean;
    /** The type of the element. */
    type?: string;
    /** The value associated with the element's name when it's submitted with form data. */
    value?: string;
    /** The appearance variant of the button. */
    variant?: IconButtonVariant;
    /** The width of the button. */
    width?: IconButtonWidth;
    /** Renders the icon of the button. */
    children?: Snippet;
    /** Renders an icon, when selected. */
    selectedSnippet?: Snippet;
    /** Dispatched when a toggle button's selected state changes. */
    oninput?: (e: Event) => void;
    /** Dispatched when a toggle button's selected state changes. */
    onchange?: (e: Event) => void;
    /** Emitted when the element is clicked. */
    onclick?: (e: MouseEvent) => void;
    element?: M3eIconButtonElement;
  }

  let { disabled, disabledInteractive, download, href, name, rel, selected, shape, size, target, toggle, type, value, variant, width, children, selectedSnippet, oninput, onchange, onclick, element = $bindable() }: Props = $props();
</script>

<!-- svelte-ignore a11y_click_events_have_key_events -->
<!-- svelte-ignore a11y_no_static_element_interactions -->
<m3e-icon-button
  bind:this={element}
  disabled={disabled || undefined}
  disabled-interactive={disabledInteractive || undefined}
  {download}
  {href}
  {name}
  {rel}
  selected={selected || undefined}
  {shape}
  {size}
  {target}
  toggle={toggle || undefined}
  {type}
  {value}
  {variant}
  {width}
  oninput={oninput}
  onchange={onchange}
  onclick={onclick}
>
  {#if selectedSnippet}<div slot="selected" style="display:contents">{@render selectedSnippet()}</div>{/if}
  {@render children?.()}
</m3e-icon-button>
