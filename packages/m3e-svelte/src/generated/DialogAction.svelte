<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/dialog");
  import type { M3eDialogActionElement } from "@m3e/dialog";

  interface Props {
    /** The value to return from the dialog. */
    returnValue?: string;
    /** Renders the content of the action. */
    children?: Snippet;
    element?: M3eDialogActionElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { returnValue, children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-dialog-action
  bind:this={element}
  {...rest}
  return-value={returnValue}
>
  {@render children?.()}
</m3e-dialog-action>
