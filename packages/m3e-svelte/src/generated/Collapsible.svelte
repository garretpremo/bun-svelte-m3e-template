<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  import { syncProperty } from "../runtime/upgrade";
  if (browser) void import("@m3e/core");
  import type { M3eCollapsibleElement } from "@m3e/core";

  interface Props {
    /** Whether content is visible. */
    open?: boolean;
    /** Renders the collapsible content. */
    children?: Snippet;
    /** Emitted when the collapsible begins to open. */
    onopening?: (e: Event) => void;
    /** Emitted when the collapsible has opened. */
    onopened?: (e: Event) => void;
    /** Emitted when the collapsible begins to close. */
    onclosing?: (e: Event) => void;
    /** Emitted when the collapsible has closed. */
    onclosed?: (e: Event) => void;
    element?: M3eCollapsibleElement;
  }

  let { open = $bindable(false), children, onopening, onopened, onclosing, onclosed, element = $bindable() }: Props = $props();

  $effect(() => {
    if (open !== undefined) syncProperty(element, "open", open);
  });
</script>

<m3e-collapsible
  bind:this={element}
  onopening={onopening}
  onopened={onopened}
  onclosing={onclosing}
  onclosed={onclosed}
>
  {@render children?.()}
</m3e-collapsible>
