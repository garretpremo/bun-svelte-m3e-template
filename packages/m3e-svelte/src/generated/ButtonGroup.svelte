<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/button-group");
  import type { M3eButtonGroupElement, ButtonGroupSize, ButtonGroupVariant } from "@m3e/button-group";

  interface Props {
    /** Whether multiple toggle buttons can be selected. */
    multi?: boolean;
    /** The size of the group. */
    size?: ButtonGroupSize;
    /** The appearance variant of the group. */
    variant?: ButtonGroupVariant;
    /** Renders the buttons of the group. */
    children?: Snippet;
    element?: M3eButtonGroupElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { multi, size, variant, children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-button-group
  bind:this={element}
  {...rest}
  multi={multi || undefined}
  {size}
  {variant}
>
  {@render children?.()}
</m3e-button-group>
