<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/menu");
  import type { M3eMenuElement, MenuPositionX, MenuPositionY, MenuVariant } from "@m3e/menu";

  interface Props {
    /** The position of the menu, on the x-axis. */
    positionX?: MenuPositionX;
    /** The position of the menu, on the y-axis. */
    positionY?: MenuPositionY;
    /** The appearance variant of the menu. */
    variant?: MenuVariant;
    /** A value indicating whether the menu is a submenu. */
    submenu?: boolean;
    /** Renders the contents of the menu. */
    children?: Snippet;
    /** Dispatched before the toggle state changes. */
    onbeforetoggle?: (e: Event) => void;
    /** Dispatched after the toggle state has changed. */
    ontoggle?: (e: Event) => void;
    element?: M3eMenuElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { positionX, positionY, variant, submenu, children, onbeforetoggle, ontoggle, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-menu
  bind:this={element}
  {...rest}
  position-x={positionX}
  position-y={positionY}
  {variant}
  submenu={submenu || undefined}
  onbeforetoggle={onbeforetoggle}
  ontoggle={ontoggle}
>
  {@render children?.()}
</m3e-menu>
