<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/core");
  import type { M3eTextHighlightElement } from "@m3e/core";

  interface Props {
    /** A value indicating whether matching is case sensitive. */
    caseSensitive?: boolean;
    /** A value indicating whether text highlighting is disabled. */
    disabled?: boolean;
    /** The term to highlight. */
    term?: string;
    /** Renders the content to highlight. */
    children?: Snippet;
    /** Emitted when content is highlighted. */
    onhighlight?: (e: CustomEvent) => void;
    element?: M3eTextHighlightElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { caseSensitive, disabled, term, children, onhighlight, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-text-highlight
  bind:this={element}
  {...rest}
  case-sensitive={caseSensitive || undefined}
  disabled={disabled || undefined}
  {term}
  onhighlight={onhighlight}
>
  {@render children?.()}
</m3e-text-highlight>
