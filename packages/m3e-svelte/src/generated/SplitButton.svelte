<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/split-button");
  import type { M3eSplitButtonElement, SplitButtonVariant } from "@m3e/split-button";

  interface Props {
    /** The appearance variant of the button. */
    variant?: SplitButtonVariant;
    /** The size of the button. */
    size?: string;
    /** The leading button used to perform the primary action. */
    leadingButton?: Snippet;
    /** The trailing icon button used to open a menu of related actions. */
    trailingButton?: Snippet;
    element?: M3eSplitButtonElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { variant, size, leadingButton, trailingButton, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-split-button
  bind:this={element}
  {...rest}
  {variant}
  {size}
>
  {#if leadingButton}<div slot="leading-button" style="display:contents">{@render leadingButton()}</div>{/if}
  {#if trailingButton}<div slot="trailing-button" style="display:contents">{@render trailingButton()}</div>{/if}
</m3e-split-button>
