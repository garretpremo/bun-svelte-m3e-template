<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/core");
  import type { M3eTextOverflowElement } from "@m3e/core";

  interface Props {
    /** Renders the content to truncate with an ellipsis. */
    children?: Snippet;
    element?: M3eTextOverflowElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-text-overflow
  bind:this={element}
  {...rest}
>
  {@render children?.()}
</m3e-text-overflow>
