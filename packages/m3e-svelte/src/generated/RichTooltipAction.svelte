<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/tooltip");
  import type { M3eRichTooltipActionElement } from "@m3e/tooltip";

  interface Props {
    /** Whether to focus should not be restored to the trigger when activated. */
    disableRestoreFocus?: boolean;
    /** Renders the content of the action. */
    children?: Snippet;
    element?: M3eRichTooltipActionElement;
  }

  let { disableRestoreFocus, children, element = $bindable() }: Props = $props();
</script>

<m3e-rich-tooltip-action
  bind:this={element}
  disable-restore-focus={disableRestoreFocus || undefined}
>
  {@render children?.()}
</m3e-rich-tooltip-action>
