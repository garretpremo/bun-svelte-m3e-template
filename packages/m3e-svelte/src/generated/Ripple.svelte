<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/core");
  import type { M3eRippleElement } from "@m3e/core";

  interface Props {
    /** Whether the ripple always originates from the center of the element's bounds, rather than originating from the location of the click event. */
    centered?: boolean;
    /** Whether click events will not trigger the ripple. Ripples can be still controlled manually by using the `show` and 'hide' methods. */
    disabled?: boolean;
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    /** The radius, in pixels, of the ripple. */
    radius?: number | null;
    /** Whether the ripple is visible outside the element's bounds. */
    unbounded?: boolean;
    element?: M3eRippleElement;
  }

  let { centered, disabled, for_, radius, unbounded, element = $bindable() }: Props = $props();
</script>

<m3e-ripple
  bind:this={element}
  centered={centered || undefined}
  disabled={disabled || undefined}
  for={for_}
  {radius}
  unbounded={unbounded || undefined}
>

</m3e-ripple>
