<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/shape");
  import type { M3eShapeElement, ShapeName } from "@m3e/shape";

  interface Props {
    /** The name of the shape. */
    name?: ShapeName | null;
    /** Renders the clipped content of the shape. */
    children?: Snippet;
    element?: M3eShapeElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { name, children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-shape
  bind:this={element}
  {...rest}
  {name}
>
  {@render children?.()}
</m3e-shape>
