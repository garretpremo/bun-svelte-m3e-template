<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/progress-indicator");
  import type { M3eLinearProgressIndicatorElement, LinearProgressMode } from "@m3e/progress-indicator";

  interface Props {
    /** A fractional value, between 0 and `max`, indicating buffer progress. */
    bufferValue?: number;
    /** The maximum progress value. */
    max?: number;
    /** The mode of the progress bar. */
    mode?: LinearProgressMode;
    /** A fractional value, between 0 and `max`, indicating progress. */
    value?: number;
    /** The appearance of the indicator. */
    variant?: string;
    element?: M3eLinearProgressIndicatorElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { bufferValue, max, mode, value, variant, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-linear-progress-indicator
  bind:this={element}
  {...rest}
  buffer-value={bufferValue}
  {max}
  {mode}
  {value}
  {variant}
>

</m3e-linear-progress-indicator>
