<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/form-field");
  import type { M3eFormFieldElement, FloatLabelType, HideSubscriptType, FormFieldVariant } from "@m3e/form-field";

  interface Props {
    /** Specifies whether the label should float always or only when necessary. */
    floatLabel?: FloatLabelType;
    /** Whether the required marker should be hidden. */
    hideRequiredMarker?: boolean;
    /** Whether subscript content is hidden. */
    hideSubscript?: HideSubscriptType;
    /** The appearance variant of the field. */
    variant?: FormFieldVariant;
    /** Renders the control of the field. */
    children?: Snippet;
    /** Renders content before the fields's control. */
    prefix?: Snippet;
    /** Renders text before the fields's control. */
    prefixText?: Snippet;
    /** Renders content after the fields's control. */
    suffix?: Snippet;
    /** Renders text after the fields's control. */
    suffixText?: Snippet;
    /** Renders hint text in the fields's subscript, when the control is valid. */
    hint?: Snippet;
    /** Renders error text in the fields's subscript, when the control is invalid. */
    error?: Snippet;
    element?: M3eFormFieldElement;
  }

  let { floatLabel, hideRequiredMarker, hideSubscript, variant, children, prefix, prefixText, suffix, suffixText, hint, error, element = $bindable() }: Props = $props();
</script>

<m3e-form-field
  bind:this={element}
  float-label={floatLabel}
  hide-required-marker={hideRequiredMarker || undefined}
  hide-subscript={hideSubscript}
  {variant}
>
  {#if prefix}<div slot="prefix" style="display:contents">{@render prefix()}</div>{/if}
  {#if prefixText}<div slot="prefix-text" style="display:contents">{@render prefixText()}</div>{/if}
  {#if suffix}<div slot="suffix" style="display:contents">{@render suffix()}</div>{/if}
  {#if suffixText}<div slot="suffix-text" style="display:contents">{@render suffixText()}</div>{/if}
  {#if hint}<div slot="hint" style="display:contents">{@render hint()}</div>{/if}
  {#if error}<div slot="error" style="display:contents">{@render error()}</div>{/if}
  {@render children?.()}
</m3e-form-field>
