<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/expansion-panel");
  import type { M3eAccordionElement } from "@m3e/expansion-panel";

  interface Props {
    /** Whether multiple expansion panels can be open at the same time. */
    multi?: boolean;
    /** Renders the panels of the accordion. */
    children?: Snippet;
    element?: M3eAccordionElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { multi, children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-accordion
  bind:this={element}
  {...rest}
  multi={multi || undefined}
>
  {@render children?.()}
</m3e-accordion>
