<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/stepper");
  import type { M3eStepPanelElement } from "@m3e/stepper";

  interface Props {
    /** Renders the content of the panel. */
    children?: Snippet;
    /** Renders the actions bar of the panel. */
    actions?: Snippet;
    element?: M3eStepPanelElement;
  }

  let { children, actions, element = $bindable() }: Props = $props();
</script>

<m3e-step-panel
  bind:this={element}
>
  {#if actions}<div slot="actions-" style="display:contents">{@render actions()}</div>{/if}
  {@render children?.()}
</m3e-step-panel>
