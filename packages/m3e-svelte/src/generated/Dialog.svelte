<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  import { syncProperty } from "../runtime/upgrade";
  if (browser) void import("@m3e/dialog");
  import type { M3eDialogElement } from "@m3e/dialog";

  interface Props {
    /** Whether the dialog is an alert. */
    alert?: boolean;
    /** The accessible label given to the button used to dismiss the dialog. */
    closeLabel?: string;
    /** Whether users cannot click the backdrop or press ESC to dismiss the dialog. */
    disableClose?: boolean;
    /** Whether a button is presented that can be used to close the dialog. */
    dismissible?: boolean;
    /** Whether to disable focus trapping, which keeps keyboard `Tab` navigation within the dialog. */
    noFocusTrap?: boolean;
    /** Whether the dialog is open. */
    open?: boolean;
    /** Renders the content of the dialog. */
    children?: Snippet;
    /** Renders the header of the dialog. */
    header?: Snippet;
    /** Renders the actions of the dialog. */
    actions?: Snippet;
    /** Renders the icon of the button used to close the dialog. */
    closeIcon?: Snippet;
    /** Emitted when the dialog begins to open. */
    onopening?: (e: Event) => void;
    /** Emitted when the dialog has opened. */
    onopened?: (e: Event) => void;
    /** Emitted when the dialog begins to close. */
    onclosing?: (e: Event) => void;
    /** Emitted when the dialog has closed. */
    onclosed?: (e: Event) => void;
    /** Emitted when the dialog is cancelled. */
    oncancel?: (e: Event) => void;
    element?: M3eDialogElement;
  }

  let { alert, closeLabel, disableClose, dismissible, noFocusTrap, open = $bindable(false), children, header, actions, closeIcon, onopening, onopened, onclosing, onclosed, oncancel, element = $bindable() }: Props = $props();

  $effect(() => {
    if (open !== undefined) syncProperty(element, "open", open);
  });
</script>

<m3e-dialog
  bind:this={element}
  alert={alert || undefined}
  close-label={closeLabel}
  disable-close={disableClose || undefined}
  dismissible={dismissible || undefined}
  no-focus-trap={noFocusTrap || undefined}
  onopening={onopening}
  onopened={onopened}
  onclosing={onclosing}
  onclosed={onclosed}
  oncancel={oncancel}
>
  {#if header}<div slot="header" style="display:contents">{@render header()}</div>{/if}
  {#if actions}<div slot="actions" style="display:contents">{@render actions()}</div>{/if}
  {#if closeIcon}<div slot="close-icon" style="display:contents">{@render closeIcon()}</div>{/if}
  {@render children?.()}
</m3e-dialog>
