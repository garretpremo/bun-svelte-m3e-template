<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/slide-group");
  import type { M3eSlideGroupElement } from "@m3e/slide-group";

  interface Props {
    /** Whether scroll buttons are disabled. */
    disabled?: boolean;
    /** The accessible label given to the button used to move to the next page. */
    nextPageLabel?: string;
    /** The accessible label given to the button used to move to the previous page. */
    previousPageLabel?: string;
    /** A value, in pixels, indicating the scroll threshold at which to begin showing pagination controls. */
    threshold?: number;
    /** Whether content is oriented vertically. */
    vertical?: boolean;
    /** Renders the content to paginate. */
    children?: Snippet;
    /** Renders the icon to present for the next button. */
    nextIcon?: Snippet;
    /** Renders the icon to present for the previous button. */
    prevIcon?: Snippet;
    element?: M3eSlideGroupElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disabled, nextPageLabel, previousPageLabel, threshold, vertical, children, nextIcon, prevIcon, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-slide-group
  bind:this={element}
  {...rest}
  disabled={disabled || undefined}
  next-page-label={nextPageLabel}
  previous-page-label={previousPageLabel}
  {threshold}
  vertical={vertical || undefined}
>
  {#if nextIcon}<div slot="next-icon" style="display:contents">{@render nextIcon()}</div>{/if}
  {#if prevIcon}<div slot="prev-icon" style="display:contents">{@render prevIcon()}</div>{/if}
  {@render children?.()}
</m3e-slide-group>
