<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  import { syncManagedProperty } from "../runtime/upgrade";
  if (browser) void import("@m3e/radio-group");
  import type { M3eRadioElement } from "@m3e/radio-group";

  interface Props {
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** The name that identifies the element when submitting the associated form. */
    name?: string;
    /** Whether the element is required. */
    required?: string;
    /** Whether the element is checked. */
    checked?: boolean;
    /** A string representing the value of the radio. */
    value?: string;
    /** Emitted when the checked state changes. */
    oninput?: (e: Event) => void;
    /** Emitted when the checked state changes. */
    onchange?: (e: Event) => void;
    /** Emitted when the element is clicked. */
    onclick?: (e: MouseEvent) => void;
    element?: M3eRadioElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disabled, name, required, checked = $bindable(false), value = $bindable(undefined), oninput, onchange, onclick, element = $bindable(), ...rest }: Props = $props();

  function syncFromDom() {
    if (!element) return;
    const node = element as unknown as Record<string, unknown>;
    { const next = node["checked"]; if (next != null) checked = next as boolean; }
    { const next = node["value"]; if (next != null) value = next as string; }
  }

  $effect(() => {
    if (checked !== undefined) syncManagedProperty(element, "checked", checked);
  });
  $effect(() => {
    if (value !== undefined) syncManagedProperty(element, "value", value);
  });
</script>

<!-- svelte-ignore a11y_click_events_have_key_events -->
<!-- svelte-ignore a11y_no_static_element_interactions -->
<m3e-radio
  bind:this={element}
  {...rest}
  disabled={disabled || undefined}
  {name}
  {required}
  oninput={(e: Event) => { syncFromDom(); oninput?.(e); }}
  onchange={(e: Event) => { syncFromDom(); onchange?.(e); }}
  onclick={onclick}
>

</m3e-radio>
