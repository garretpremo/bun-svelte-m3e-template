<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/menu");
  import type { M3eMenuTriggerElement } from "@m3e/menu";

  interface Props {
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    /** Renders the contents of the trigger. */
    children?: Snippet;
    element?: M3eMenuTriggerElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { for_, children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-menu-trigger
  bind:this={element}
  {...rest}
  for={for_}
>
  {@render children?.()}
</m3e-menu-trigger>
