<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/slider");
  import type { M3eSliderThumbElement } from "@m3e/slider";

  interface Props {
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** The name that identifies the element when submitting the associated form. */
    name?: string;
    /** The value of the thumb. */
    value?: number | null;
    /** Emitted when the value changes. */
    oninput?: (e: Event) => void;
    /** Emitted when the value changes. */
    onchange?: (e: Event) => void;
    /** Emitted when the element is clicked. */
    onclick?: (e: Event) => void;
    element?: M3eSliderThumbElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disabled, name, value, oninput, onchange, onclick, element = $bindable(), ...rest }: Props = $props();
</script>

<!-- svelte-ignore a11y_click_events_have_key_events -->
<!-- svelte-ignore a11y_no_static_element_interactions -->
<m3e-slider-thumb
  bind:this={element}
  {...rest}
  disabled={disabled || undefined}
  {name}
  {value}
  oninput={oninput}
  onchange={onchange}
  onclick={onclick}
>

</m3e-slider-thumb>
