<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/segmented-button");
  import type { M3eSegmentedButtonElement } from "@m3e/segmented-button";

  interface Props {
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** Whether to hide the selection indicator. */
    hideSelectionIndicator?: boolean;
    /** Whether multiple options can be selected. */
    multi?: boolean;
    /** The name that identifies the element when submitting the associated form. */
    name?: string;
    /** Renders the segments of the button. */
    children?: Snippet;
    /** Emitted when the checked state of a segment changes. */
    onchange?: (e: Event) => void;
    /** Emitted when the checked state of a segment changes. */
    oninput?: (e: Event) => void;
    element?: M3eSegmentedButtonElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disabled, hideSelectionIndicator, multi, name, children, onchange, oninput, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-segmented-button
  bind:this={element}
  {...rest}
  disabled={disabled || undefined}
  hide-selection-indicator={hideSelectionIndicator || undefined}
  multi={multi || undefined}
  {name}
  onchange={onchange}
  oninput={oninput}
>
  {@render children?.()}
</m3e-segmented-button>
