<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/chips");
  import type { M3eInputChipElement, ChipVariant } from "@m3e/chips";

  interface Props {
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** Whether the element is disabled and interactive. */
    disabledInteractive?: boolean;
    /** Whether the chip is removable. */
    removable?: boolean;
    /** The accessible label given to the button used to remove the chip. */
    removeLabel?: string;
    /** A string representing the value of the chip. */
    value?: string;
    /** The appearance variant of the chip. */
    variant?: ChipVariant;
    /** Renders the label of the chip. */
    children?: Snippet;
    /** Renders an avatar before the chip's label. */
    avatar?: Snippet;
    /** Renders an icon before the chip's label. */
    icon?: Snippet;
    /** Renders the icon for the button used to remove the chip. */
    removeIcon?: Snippet;
    /** Renders an icon after the chip's label. */
    trailingIcon?: Snippet;
    /** Emitted when the remove button is clicked or DELETE or BACKSPACE key is pressed. */
    onremove?: (e: Event) => void;
    /** Emitted when the element is clicked. */
    onclick?: (e: Event) => void;
    element?: M3eInputChipElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disabled, disabledInteractive, removable, removeLabel, value, variant, children, avatar, icon, removeIcon, trailingIcon, onremove, onclick, element = $bindable(), ...rest }: Props = $props();
</script>

<!-- svelte-ignore a11y_click_events_have_key_events -->
<!-- svelte-ignore a11y_no_static_element_interactions -->
<m3e-input-chip
  bind:this={element}
  {...rest}
  disabled={disabled || undefined}
  disabled-interactive={disabledInteractive || undefined}
  removable={removable || undefined}
  remove-label={removeLabel}
  {value}
  {variant}
  onremove={onremove}
  onclick={onclick}
>
  {#if avatar}<div slot="avatar" style="display:contents">{@render avatar()}</div>{/if}
  {#if icon}<div slot="icon" style="display:contents">{@render icon()}</div>{/if}
  {#if removeIcon}<div slot="remove-icon" style="display:contents">{@render removeIcon()}</div>{/if}
  {#if trailingIcon}<div slot="trailing-icon" style="display:contents">{@render trailingIcon()}</div>{/if}
  {@render children?.()}
</m3e-input-chip>
