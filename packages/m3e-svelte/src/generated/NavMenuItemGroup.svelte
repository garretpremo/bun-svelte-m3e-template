<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  import { syncManagedProperty } from "../runtime/upgrade";
  if (browser) void import("@m3e/nav-menu");
  import type { M3eNavMenuItemGroupElement } from "@m3e/nav-menu";

  interface Props {
    /** Renders the items of the group. */
    children?: Snippet;
    /** Renders the label of the group. */
    label?: Snippet;
    element?: M3eNavMenuItemGroupElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { children, label, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-nav-menu-item-group
  bind:this={element}
  {...rest}
>
  {#if label}<div slot="label" style="display:contents">{@render label()}</div>{/if}
  {@render children?.()}
</m3e-nav-menu-item-group>
