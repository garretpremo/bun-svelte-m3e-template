<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/core");
  import type { M3eScrollContainerElement, ScrollDividers } from "@m3e/core";

  interface Props {
    /** The dividers used to separate scrollable content. */
    dividers?: ScrollDividers;
    /** Whether to present thin scrollbars. */
    thin?: boolean;
    /** Renders the scrollable content. */
    children?: Snippet;
    element?: M3eScrollContainerElement;
  }

  let { dividers, thin, children, element = $bindable() }: Props = $props();
</script>

<m3e-scroll-container
  bind:this={element}
  {dividers}
  thin={thin || undefined}
>
  {@render children?.()}
</m3e-scroll-container>
