<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/list");
  import type { M3eListItemButtonElement } from "@m3e/list";

  interface Props {
    /** The URL to which the link button points. */
    href?: string;
    /** The target of the link button. */
    target?: string;
    /** The relationship between the `target` of the link button and the document. */
    rel?: string;
    /** A value indicating whether the `target` of the link button will be downloaded, optionally specifying the new name of the file. */
    download?: string | null;
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** Renders the content of the list item. */
    children?: Snippet;
    /** Renders the leading content of the list item. */
    leading?: Snippet;
    /** Renders the overline of the list item. */
    overline?: Snippet;
    /** Renders the supporting text of the list item. */
    supportingText?: Snippet;
    /** Renders the trailing content of the list item. */
    trailing?: Snippet;
    onclick?: (e: MouseEvent) => void;
    element?: M3eListItemButtonElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { href, target, rel, download, disabled, children, leading, overline, supportingText, trailing, onclick, element = $bindable(), ...rest }: Props = $props();
</script>

<!-- svelte-ignore a11y_click_events_have_key_events -->
<!-- svelte-ignore a11y_no_static_element_interactions -->
<m3e-list-item-button
  bind:this={element}
  {...rest}
  {href}
  {target}
  {rel}
  {download}
  disabled={disabled || undefined}
  onclick={onclick}
>
  {#if leading}<div slot="leading" style="display:contents">{@render leading()}</div>{/if}
  {#if overline}<div slot="overline" style="display:contents">{@render overline()}</div>{/if}
  {#if supportingText}<div slot="supporting-text" style="display:contents">{@render supportingText()}</div>{/if}
  {#if trailing}<div slot="trailing" style="display:contents">{@render trailing()}</div>{/if}
  {@render children?.()}
</m3e-list-item-button>
