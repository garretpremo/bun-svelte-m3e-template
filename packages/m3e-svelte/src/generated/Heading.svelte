<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/heading");
  import type { M3eHeadingElement, HeadingLevel, HeadingSize, HeadingVariant } from "@m3e/heading";

  interface Props {
    /** Whether the heading uses an emphasized typescale. */
    emphasized?: boolean;
    /** The accessibility level of the heading. */
    level?: HeadingLevel | undefined;
    /** The size of the heading. */
    size?: HeadingSize;
    /** The appearance variant of the heading. */
    variant?: HeadingVariant;
    /** Renders the content of the heading. */
    children?: Snippet;
    element?: M3eHeadingElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { emphasized, level, size, variant, children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-heading
  bind:this={element}
  {...rest}
  emphasized={emphasized || undefined}
  {level}
  {size}
  {variant}
>
  {@render children?.()}
</m3e-heading>
