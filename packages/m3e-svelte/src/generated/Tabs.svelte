<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/tabs");
  import type { M3eTabsElement, TabHeaderPosition, TabVariant } from "@m3e/tabs";

  interface Props {
    /** Whether scroll buttons are disabled. */
    disablePagination?: boolean;
    /** The position of the tab headers. */
    headerPosition?: TabHeaderPosition;
    /** The accessible label given to the button used to move to the next page. */
    nextPageLabel?: string;
    /** The accessible label given to the button used to move to the previous page. */
    previousPageLabel?: string;
    /** Whether tabs are stretched to fill the header. */
    stretch?: boolean;
    /** The appearance variant of the tabs. */
    variant?: TabVariant;
    /** Renders the tabs. */
    children?: Snippet;
    /** Renders the panels of the tabs. */
    panel?: Snippet;
    /** Renders the icon to present for the next button used to paginate. */
    nextIcon?: Snippet;
    /** Renders the icon to present for the previous button used to paginate. */
    prevIcon?: Snippet;
    /** Emitted when the selected tab changes. */
    onchange?: (e: Event) => void;
    element?: M3eTabsElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disablePagination, headerPosition, nextPageLabel, previousPageLabel, stretch, variant, children, panel, nextIcon, prevIcon, onchange, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-tabs
  bind:this={element}
  {...rest}
  disable-pagination={disablePagination || undefined}
  header-position={headerPosition}
  next-page-label={nextPageLabel}
  previous-page-label={previousPageLabel}
  stretch={stretch || undefined}
  {variant}
  onchange={onchange}
>
  {#if panel}<div slot="panel" style="display:contents">{@render panel()}</div>{/if}
  {#if nextIcon}<div slot="next-icon" style="display:contents">{@render nextIcon()}</div>{/if}
  {#if prevIcon}<div slot="prev-icon" style="display:contents">{@render prevIcon()}</div>{/if}
  {@render children?.()}
</m3e-tabs>
