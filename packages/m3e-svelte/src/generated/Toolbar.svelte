<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/toolbar");
  import type { M3eToolbarElement, ToolbarShape, ToolbarVariant } from "@m3e/toolbar";

  interface Props {
    /** Whether the toolbar is elevated. */
    elevated?: boolean;
    /** The shape of the toolbar. */
    shape?: ToolbarShape;
    /** The appearance variant of the toolbar. */
    variant?: ToolbarVariant;
    /** Whether the element is oriented vertically. */
    vertical?: boolean;
    /** Renders the content of the toolbar. */
    children?: Snippet;
    element?: M3eToolbarElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { elevated, shape, variant, vertical, children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-toolbar
  bind:this={element}
  {...rest}
  elevated={elevated || undefined}
  {shape}
  {variant}
  vertical={vertical || undefined}
>
  {@render children?.()}
</m3e-toolbar>
