<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/card");
  import type { M3eCardElement, CardOrientation, CardVariant } from "@m3e/card";

  interface Props {
    /** Whether the card is "actionable" and will respond to use interaction. */
    actionable?: boolean;
    /** Whether to present the card inline with surrounding content. */
    inline?: boolean;
    /** The orientation of the card. */
    orientation?: CardOrientation;
    /** The appearance variant of the card. */
    variant?: CardVariant;
    /** The URL to which the link button points. */
    href?: string;
    /** The target of the link button. */
    target?: string;
    /** The relationship between the `target` of the link button and the document. */
    rel?: string;
    /** A value indicating whether the `target` of the link button will be downloaded, optionally specifying the new name of the file. */
    download?: string | null;
    /** The name of the element, submitted as a pair with the element's `value` as part of form data, when the element is used to submit a form. */
    name?: string;
    /** The value associated with the element's name when it's submitted with form data. */
    value?: string;
    /** The type of the element. */
    type?: string;
    /** Whether the element is disabled and interactive. */
    disabledInteractive?: boolean;
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** Renders the content of the card without padding. */
    children?: Snippet;
    /** Renders the header of the card. */
    header?: Snippet;
    /** Renders the content of the card with padding. */
    content?: Snippet;
    /** Renders the actions of the card. */
    actions?: Snippet;
    /** Renders the footer of the card. */
    footer?: Snippet;
    /** Emitted when the element is clicked. */
    onclick?: (e: MouseEvent) => void;
    element?: M3eCardElement;
  }

  let { actionable, inline, orientation, variant, href, target, rel, download, name, value, type, disabledInteractive, disabled, children, header, content, actions, footer, onclick, element = $bindable() }: Props = $props();
</script>

<!-- svelte-ignore a11y_click_events_have_key_events -->
<!-- svelte-ignore a11y_no_static_element_interactions -->
<m3e-card
  bind:this={element}
  actionable={actionable || undefined}
  inline={inline || undefined}
  {orientation}
  {variant}
  {href}
  {target}
  {rel}
  {download}
  {name}
  {value}
  {type}
  disabled-interactive={disabledInteractive || undefined}
  disabled={disabled || undefined}
  onclick={onclick}
>
  {#if header}<div slot="header" style="display:contents">{@render header()}</div>{/if}
  {#if content}<div slot="content" style="display:contents">{@render content()}</div>{/if}
  {#if actions}<div slot="actions" style="display:contents">{@render actions()}</div>{/if}
  {#if footer}<div slot="footer" style="display:contents">{@render footer()}</div>{/if}
  {@render children?.()}
</m3e-card>
