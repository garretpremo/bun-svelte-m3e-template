<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/core");
  import type { M3eFocusRingElement } from "@m3e/core";

  interface Props {
    /** Whether the focus events will not trigger the focus ring. Focus rings can be still controlled manually by using the `show` and `hide` methods. */
    disabled?: boolean;
    /** Whether the focus ring animates inward instead of outward. */
    inward?: boolean;
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    element?: M3eFocusRingElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disabled, inward, for_, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-focus-ring
  bind:this={element}
  {...rest}
  disabled={disabled || undefined}
  inward={inward || undefined}
  for={for_}
>

</m3e-focus-ring>
