<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/fab");
  import type { M3eFabElement, FabSize, FabVariant } from "@m3e/fab";

  interface Props {
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** Whether the element is disabled and interactive. */
    disabledInteractive?: boolean;
    /** A value indicating whether the `target` of the link button will be downloaded, optionally specifying the new name of the file. */
    download?: string | null;
    /** Whether the button is extended to show the label. */
    extended?: boolean;
    /** The URL to which the link button points. */
    href?: string;
    /** Whether to present a lowered elevation. */
    lowered?: boolean;
    /** The name of the element, submitted as a pair with the element's `value` as part of form data, when the element is used to submit a form. */
    name?: string;
    /** The relationship between the `target` of the link button and the document. */
    rel?: string;
    /** The size of the button. */
    size?: FabSize;
    /** The target of the link button. */
    target?: string;
    /** The type of the element. */
    type?: string;
    /** The value associated with the element's name when it's submitted with form data. */
    value?: string;
    /** The appearance variant of the button. */
    variant?: FabVariant;
    /** Renders the icon of the button. */
    children?: Snippet;
    /** Renders the label of an extended button. */
    label?: Snippet;
    /** Renders the close icon when used to open a FAB menu. */
    closeIcon?: Snippet;
    /** Emitted when the element is clicked. */
    onclick?: (e: MouseEvent) => void;
    element?: M3eFabElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disabled, disabledInteractive, download, extended, href, lowered, name, rel, size, target, type, value, variant, children, label, closeIcon, onclick, element = $bindable(), ...rest }: Props = $props();
</script>

<!-- svelte-ignore a11y_click_events_have_key_events -->
<!-- svelte-ignore a11y_no_static_element_interactions -->
<m3e-fab
  bind:this={element}
  {...rest}
  disabled={disabled || undefined}
  disabled-interactive={disabledInteractive || undefined}
  {download}
  extended={extended || undefined}
  {href}
  lowered={lowered || undefined}
  {name}
  {rel}
  {size}
  {target}
  {type}
  {value}
  {variant}
  onclick={onclick}
>
  {#if label}<div slot="label" style="display:contents">{@render label()}</div>{/if}
  {#if closeIcon}<div slot="close-icon" style="display:contents">{@render closeIcon()}</div>{/if}
  {@render children?.()}
</m3e-fab>
