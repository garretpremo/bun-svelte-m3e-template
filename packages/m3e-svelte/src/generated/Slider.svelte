<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/slider");
  import type { M3eSliderElement, SliderSize } from "@m3e/slider";

  interface Props {
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** Whether to show tick marks. */
    discrete?: boolean;
    /** Whether to show value labels when activated. */
    labelled?: boolean;
    /** The maximum allowable value. */
    max?: number;
    /** The minimum allowable value. */
    min?: number;
    /** The value at which the thumb will snap. */
    step?: number;
    /** The size of the slider. */
    size?: SliderSize;
    element?: M3eSliderElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disabled, discrete, labelled, max, min, step, size, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-slider
  bind:this={element}
  {...rest}
  disabled={disabled || undefined}
  discrete={discrete || undefined}
  labelled={labelled || undefined}
  {max}
  {min}
  {step}
  {size}
>

</m3e-slider>
