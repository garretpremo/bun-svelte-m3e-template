<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/divider");
  import type { M3eDividerElement } from "@m3e/divider";

  interface Props {
    /** Whether the divider is indented with equal padding on both sides. */
    inset?: boolean;
    /** Whether the divider is indented with padding on the leading side. */
    insetStart?: boolean;
    /** Whether the divider is indented with padding on the trailing side. */
    insetEnd?: boolean;
    /** Whether the divider is vertically aligned with adjacent content. */
    vertical?: boolean;
    element?: M3eDividerElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { inset, insetStart, insetEnd, vertical, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-divider
  bind:this={element}
  {...rest}
  inset={inset || undefined}
  inset-start={insetStart || undefined}
  inset-end={insetEnd || undefined}
  vertical={vertical || undefined}
>

</m3e-divider>
