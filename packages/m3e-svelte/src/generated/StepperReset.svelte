<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/stepper");
  import type { M3eStepperResetElement } from "@m3e/stepper";

  interface Props {
    /** Renders the content of the action. */
    children?: Snippet;
    element?: M3eStepperResetElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-stepper-reset
  bind:this={element}
  {...rest}
>
  {@render children?.()}
</m3e-stepper-reset>
