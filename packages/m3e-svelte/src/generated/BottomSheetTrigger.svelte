<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/bottom-sheet");
  import type { M3eBottomSheetTriggerElement } from "@m3e/bottom-sheet";

  interface Props {
    /** The zero‑based index of the detent the sheet should open to. */
    detent?: number | undefined;
    /** Marks this trigger as a secondary trigger for accessibility. Secondary triggers do not receive ARIA ownership. */
    secondary?: boolean;
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    /** Renders the content of the trigger. */
    children?: Snippet;
    element?: M3eBottomSheetTriggerElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { detent, secondary, for_, children, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-bottom-sheet-trigger
  bind:this={element}
  {...rest}
  {detent}
  secondary={secondary || undefined}
  for={for_}
>
  {@render children?.()}
</m3e-bottom-sheet-trigger>
