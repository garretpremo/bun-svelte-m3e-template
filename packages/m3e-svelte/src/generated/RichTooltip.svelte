<!-- @generated by scripts/generate.ts — do not edit -->
<script lang="ts">
  import type { Snippet } from "svelte";
  import { browser } from "../runtime/env";
  if (browser) void import("@m3e/tooltip");
  import type { M3eRichTooltipElement, RichTooltipPosition, TooltipTouchGestures } from "@m3e/tooltip";

  interface Props {
    /** Whether the element is disabled. */
    disabled?: boolean;
    /** The identifier of the interactive control to which this element is attached. */
    for_?: string | null;
    /** The amount of time, in milliseconds, before hiding the tooltip. */
    hideDelay?: number;
    /** The position of the tooltip. */
    position?: RichTooltipPosition;
    /** The amount of time, in milliseconds, before showing the tooltip. */
    showDelay?: number;
    /** The mode in which to handle touch gestures. */
    touchGestures?: TooltipTouchGestures;
    /** The main supporting text of the tooltip. */
    children?: Snippet;
    /** Optional subhead text displayed above the supporting content. */
    subhead?: Snippet;
    /** Optional action elements displayed at the bottom of the tooltip. */
    actions?: Snippet;
    /** Dispatched before the toggle state changes. */
    onbeforetoggle?: (e: Event) => void;
    /** Dispatched after the toggle state has changed. */
    ontoggle?: (e: Event) => void;
    element?: M3eRichTooltipElement;
    // biome-ignore lint/suspicious/noExplicitAny: pass-through attrs
    [key: string]: any;
  }

  let { disabled, for_, hideDelay, position, showDelay, touchGestures, children, subhead, actions, onbeforetoggle, ontoggle, element = $bindable(), ...rest }: Props = $props();
</script>

<m3e-rich-tooltip
  bind:this={element}
  {...rest}
  disabled={disabled || undefined}
  for={for_}
  hide-delay={hideDelay}
  {position}
  show-delay={showDelay}
  touch-gestures={touchGestures}
  onbeforetoggle={onbeforetoggle}
  ontoggle={ontoggle}
>
  {#if subhead}<div slot="subhead" style="display:contents">{@render subhead()}</div>{/if}
  {#if actions}<div slot="actions" style="display:contents">{@render actions()}</div>{/if}
  {@render children?.()}
</m3e-rich-tooltip>
